[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pokerl"
version = "0.1.0"
description = "A benchmark study of RL algorithms for Pokemon battles on Pokemon Showdown"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
